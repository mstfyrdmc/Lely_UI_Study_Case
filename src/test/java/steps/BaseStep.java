package steps;

import pageModel.BaseModel;

import java.util.logging.Logger;

public class BaseStep extends BaseModel {
    public static Logger log = Logger.getLogger(String.valueOf(BaseStep.class));

}
